<Window x:Class="Food_Ordering.shipper.Shipper"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Food_Ordering.shipper"
        mc:Ignorable="d"
        Title="Shipper" Height="500" Width="1000">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="dgOrders"
                  Grid.Column="0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionChanged="dgOrders_SelectionChanged"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Restaurant ID" Binding="{Binding RestaurantId}" Width="100"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount}" Width="100"/>
                <DataGridTextColumn Header="Delivery Address" Binding="{Binding DeliveryAddress}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Column="2"
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center" Text="Order ID"/>
                <TextBox x:Name="txtOrderId" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" IsReadOnly="True"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center" Text="Restaurant ID"/>
                <TextBox x:Name="txtRestaurantId" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" IsReadOnly="True"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center" Text="Total Amount"/>
                <TextBox x:Name="txtTotalAmount" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" IsReadOnly="True"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Top" Text="Delivery Address"/>
                <TextBox x:Name="txtDeliveryAddress"
                         Grid.Row="3" Grid.Column="1"
                         Margin="0,0,0,10"
                         Height="60"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Top" Text="Note"/>
                <TextBox x:Name="txtNote"
                         Grid.Row="4" Grid.Column="1"
                         Margin="0,0,0,10"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True"/>

                <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnReceive"
                         Content="Receive"
                         Width="90"
                         Margin="5"
                        Click="btnReceive_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>